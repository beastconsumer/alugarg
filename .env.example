# Optional local configuration example
# The app currently reads MAPS_API_KEY from android/local.properties via manifest placeholders.
# Keep this file only as reference if you decide to use flutter_dotenv later.

MAPS_API_KEY=YOUR_MAPS_API_KEY
SUPABASE_URL=YOUR_SUPABASE_URL
SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY
SUPABASE_BUCKET=property-images
