// Push notifications disabled until Firebase (google-services.json) is configured.
// To re-enable: npm install @capacitor/push-notifications and restore the full implementation.
export function usePushNotifications() {
  // noop
}
